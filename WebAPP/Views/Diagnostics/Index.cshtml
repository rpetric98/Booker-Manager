@model IEnumerable<WebAPP.Models.DiagnosticsVM>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<table class="table">
        <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PropertyCount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PathFileCount)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.PropertyCount)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.PathFileCount)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { /*id=item.PrimaryKey*/ }, new { @class = "btn btn-primary" }) |
                    @Html.ActionLink("Details", "Details", new {/*id=item.PrimaryKey*/}, new { @class = "btn btn-danger" })
                    @Html.ActionLink("Delete", "Delete", new { /*id=item.PrimaryKey*/ }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody
</table>
